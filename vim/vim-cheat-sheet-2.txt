NORMAL-MODE
============
Buffer:

	<C-6> = next buffer
	<C-^> = next buffer (Englisch keyboard)

COMMANDLINE-MODE (:)
=====================
Buffer:
	bnext = next buffer
	bprev = previous buffer
	buffer <n> = switch to buffer <n>
